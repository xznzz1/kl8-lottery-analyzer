# KL8彩票分析器 - 关键假设与设计决策

> 本文档记录了从原项目分离过程中的关键假设、设计决策和权衡考虑。

---

## 🎯 项目分离假设

### 1. 原始项目结构假设
- **假设**：原项目为通用彩票分析系统，支持多种彩票类型
- **决策**：专注于KL8（快乐8）彩票，移除其他彩票类型的支持
- **影响**：简化了配置管理，但失去了通用性

### 2. 数据源假设
- **假设**：原项目有稳定的数据获取API或爬虫机制
- **决策**：在`data_fetcher.py`中提供占位符实现，等待真实API接入
- **风险**：当前无法获取真实历史数据，仅用于架构验证

### 3. 算法模式假设
- **假设**：原项目有8种高级算法的完整实现
- **决策**：保留算法框架，但部分高级功能可能需要重新实现
- **影响**：`advanced_mode 2`可能暂时无法达到文档宣称的性能

---

## 📁 模块重组决策

### 1. 配置管理重构
- **原始**：可能使用全局变量和字典配置
- **现在**：使用`dataclass`和`YAML`配置，符合现代Python最佳实践
- **权衡**：增加了类型安全性，但可能与原始代码不完全兼容

### 2. CLI接口标准化
- **原始**：可能是单一脚本入口
- **现在**：分层CLI架构，符合`click`/`argparse`最佳实践
- **好处**：更好的可维护性和可扩展性

### 3. 包结构优化
```
原始推测结构:
- kl8_analysis.py (单一大文件)
- common.py (通用函数)
- config.py (全局配置)

现在结构:
src/kl8_analyzer/
├── cli/ (命令行接口)
├── core/ (核心算法)
└── utils/ (工具模块)
```

---

## 🔧 技术栈假设

### 1. 依赖管理
- **假设**：原项目使用`requirements.txt`管理依赖
- **决策**：升级到`pyproject.toml`，支持现代Python包管理
- **影响**：可能需要调整某些依赖版本

### 2. 数据科学依赖
- **保留**：`numpy`, `pandas`, `scikit-learn`
- **可选**：`tensorflow`, `keras`（仅在高级模式需要时）
- **新增**：`loguru`（替代原始logging），`pyyaml`（配置管理）

### 3. 开发工具
- **新增**：`black`, `isort`, `flake8`, `mypy`, `pytest`
- **目标**：符合AGENTS.md中的代码质量要求

---

## 🚀 性能与算法假设

### 1. 算法复杂度
- **假设**：原始算法在`O(n²)`到`O(n³)`时间复杂度范围
- **优化**：保持核心算法逻辑，优化数据结构和并行处理
- **期望**：通过工程优化提升10-30%性能

### 2. 内存使用
- **假设**：历史数据规模在100MB以内
- **决策**：使用`pandas`进行内存优化，按需加载数据
- **限制**：大规模历史数据可能需要分批处理

### 3. 并发处理
- **保留**：原有的多线程/多进程设计
- **改进**：使用`concurrent.futures`替代原始threading
- **考虑**：未来可考虑异步处理（`asyncio`）

---

## 📊 数据格式假设

### 1. 历史数据格式
```python
# 假设的CSV格式
{
    'issue': '2025001',           # 期号
    'numbers': '01,05,12,...',    # 开奖号码（逗号分隔）
    'draw_date': '2025-01-01'     # 开奖日期
}
```

### 2. 结果输出格式
- **输入兼容**：保持与原始脚本相同的参数接口
- **输出优化**：结构化JSON输出，便于后续处理
- **文件格式**：保持CSV格式兼容性

---

## ⚠️ 风险评估与缓解

### 1. 高风险假设
- **数据源依赖**：当前使用占位符数据
  - 缓解：提供标准接口，便于后续接入真实数据源
- **算法完整性**：部分高级算法可能不完整
  - 缓解：分阶段实现，优先保证基础功能

### 2. 中等风险假设
- **性能指标**：文档中的性能数据可能需要重新验证
  - 缓解：建立基准测试，逐步优化到目标性能
- **配置兼容性**：新配置系统与原始代码的兼容性
  - 缓解：提供配置迁移工具和兼容性层

### 3. 低风险假设
- **代码风格**：原始代码可能不符合PEP8
  - 缓解：使用自动化工具统一代码风格
- **测试覆盖**：原始项目可能缺少测试
  - 缓解：补充单元测试和集成测试

---

## 🔄 迭代计划

### ✅ 阶段1：基础架构（已完成）
- [x] 项目结构重组
- [x] 基础依赖管理  
- [x] CLI接口框架
- [x] 基础算法验证
- [x] 导入错误修复
- [x] 模块化运行支持

### 阶段2：核心功能（当前）
- [ ] 真实数据源接入
- [x] 核心算法完整性验证（基础功能）
- [ ] 基础性能测试

### 阶段3：高级功能
- [ ] 8种高级算法完整实现
- [ ] 性能优化和并行处理
- [ ] 完整的测试覆盖

### 阶段4：生产就绪
- [ ] 文档完善
- [ ] 性能基准验证
- [ ] 部署和运维支持

---

## 📝 待确认项目

需要从原项目确认或补充的文件/信息：

1. **真实的数据获取API或爬虫代码**
2. **完整的算法实现细节**（特别是高级模式）
3. **历史测试数据和基准性能数据**
4. **任何自定义的机器学习模型文件**
5. **配置文件模板和默认参数**

---

*本文档将随着项目进展持续更新。如发现假设不准确或需要调整，请及时更新此文档。*