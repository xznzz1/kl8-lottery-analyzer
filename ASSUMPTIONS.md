# 开发假设与约束

1. **仅支持快乐 8**  
   为满足用户“以 `src/analysis` 为核心”的要求，项目所有配置与数据抓取逻辑都限定在快乐 8（`kl8`）玩法；其他彩票类型在现阶段不做维护。

2. **Python 3.11 环境**  
   默认假设代理及研发成员均在名为 `python311` 的 Conda 环境中执行命令；若使用其他虚拟环境，请确保 Python 版本为 3.11 且依赖与此仓库一致。

3. **离线演示数据有限**  
   仓库随附的 `data/kl8/data.csv` 仅包含 3 期样例号码，用于 CI 与快速演示；任何生产分析仍需通过 `scripts/get_data.py` 获取最新数据。

4. **外部网络访问受限**  
   `src/data_fetcher.py` 中的 HTTP 客户端只允许访问白名单域名（500.com 与 917500），并依赖 YAML 中的超时/重试配置。无法访问这两个域名时，数据下载功能将失败。

5. **分析脚本作为命令行入口**  
   `src/analysis` 下的大型脚本保持原有 CLI 形态，默认从 CSV 中读取数据；导入这些模块时若存在额外参数，会按 argparse 规则解析（测试时请通过 subprocess 或设置默认参数）。
6. **Dirichlet 与规则参数配置**  
   默认假设用户沿用仓库给出的 `config.analysis` 先验设置（对称 Dirichlet 强度、方差惩罚、频繁项集阈值）并按需通过 CLI 覆盖；如需启用更激进的阈值，请同步评估历史样本量与缓存命中率。
